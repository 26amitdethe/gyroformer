# gyroformer

Predicting stellar age with uncertainty directly from photometric light curves using a transformer-based encoder. Bypasses the information bottleneck of derived rotation periods by learning directly from raw TESS/Kepler/K2 time series.
